import express from'express';import cors from'cors';import helmet from'helmet';import{pool}from'./db.js';import authRoutes from'./routes/auth.routes.js';import internshipRoutes from'./routes/internship.routes.js';import studentRoutes from'./routes/student.routes.js';import adminRoutes from'./routes/admin.routes.js';import{asyncRoute}from'./middleware/auth.js';import{notFound,errorHandler}from'./middleware/errorHandler.js';
const app=express();app.disable('x-powered-by');app.use(helmet());app.use(cors({origin:process.env.CORS_ORIGIN||'http://localhost:5173'}));app.use(express.json({limit:'1mb'}));app.get('/api/health',asyncRoute(async(_q,s)=>{await pool.query('SELECT 1');s.json({status:'ok',service:'campushire-api'})}));app.get('/api/meta',asyncRoute(async(_q,s)=>{const[departments]=await pool.query('SELECT * FROM departments ORDER BY name');const[skills]=await pool.query('SELECT * FROM skills ORDER BY name');s.json({departments,skills})}));app.use('/api/auth',authRoutes);app.use('/api/internships',internshipRoutes);app.use('/api/student',studentRoutes);app.use('/api/admin',adminRoutes);app.use(notFound);app.use(errorHandler);export default app;
