import {pool} from '../db.js';
export async function adminAnalytics(){const [[totals]]=await pool.query("SELECT (SELECT COUNT(*) FROM students) students,(SELECT COUNT(*) FROM companies) companies,(SELECT COUNT(*) FROM internships WHERE status='active' AND application_deadline>NOW()) activeInternships,(SELECT COUNT(*) FROM applications) applications,(SELECT COUNT(*) FROM interviews) interviews,(SELECT COUNT(*) FROM offers) offers,(SELECT COUNT(*) FROM offers WHERE status='accepted') acceptedOffers,(SELECT COUNT(*) FROM internship_records WHERE status='completed') completedInternships");totals.placementRate=totals.students?Math.round(10000*totals.acceptedOffers/totals.students)/100:0;const [skills]=await pool.query('SELECT name,internships_requiring_skill demand FROM skill_demand_statistics ORDER BY demand DESC LIMIT 10');const [departments]=await pool.query('SELECT * FROM placement_statistics ORDER BY applications DESC');const [companies]=await pool.query('SELECT * FROM company_recruitment_summary ORDER BY applications DESC LIMIT 10');const [funnel]=await pool.query('SELECT * FROM application_funnel_statistics');const [applicationTrend]=await pool.query("SELECT DATE_FORMAT(applied_at,'%Y-%m') month,COUNT(*) value FROM applications GROUP BY month ORDER BY month");const [offerTrend]=await pool.query("SELECT DATE_FORMAT(offered_at,'%Y-%m') month,COUNT(*) value FROM offers GROUP BY month ORDER BY month");return{totals,skills,departments,companies,funnel:funnel[0],applicationTrend,offerTrend};}
