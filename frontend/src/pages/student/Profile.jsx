import React,{useEffect,useState}from'react';import Layout from'../../components/Layout.jsx';import{api}from'../../services/api.js';import{LoadingState,ErrorState}from'../../components/UI.jsx';export default function Profile(){const[x,setX]=useState(),[message,setMessage]=useState(''),[error,setError]=useState('');useEffect(()=>{api('/student/profile').then(setX).catch(e=>setError(e.message))},[]);async function save(e){e.preventDefault();const f=Object.fromEntries(new FormData(e.currentTarget));try{await api('/student/profile',{method:'PUT',body:JSON.stringify({...f,cgpa:f.cgpa?+f.cgpa:null})});setMessage('Profile saved.')}catch(e){setError(e.message)}}if(!x)return <Layout><main className="page">{error?<ErrorState message={error}/>:<LoadingState/>}</main></Layout>;return <Layout><main className="page"><h1>Profile</h1><form className="panel" onSubmit={save}><label>First name<input name="firstName" defaultValue={x.first_name}/></label><label>Last name<input name="lastName" defaultValue={x.last_name||''}/></label><label>CGPA<input name="cgpa" type="number" min="0" max="10" step="0.01" defaultValue={x.cgpa||''}/></label><label>Phone<input name="phone" defaultValue={x.phone||''}/></label><label>Resume URL<input name="resumeUrl" type="url" defaultValue={x.resume_url||''}/></label><label>Bio<textarea name="bio" defaultValue={x.bio||''}/></label><button>Save profile</button></form>{message&&<p className="notice">{message}</p>}</main></Layout>}
