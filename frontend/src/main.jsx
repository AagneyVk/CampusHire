import React,{useEffect,useState} from 'react';import{createRoot}from'react-dom/client';import{BrowserRouter,Routes,Route,Link}from'react-router-dom';import{Search,Briefcase,Building2,Database}from'lucide-react';import'./styles.css';
const API=import.meta.env.VITE_API_URL||'http://localhost:4000/api';
function Layout({children}){return <><header><Link className="brand" to="/"><Database size={22}/> CampusHire</Link><nav><Link to="/internships">Internships</Link><Link to="/login">Login</Link></nav></header>{children}<footer>CampusHire · DBMS-focused recruitment management</footer></>}
function Home(){return <Layout><main className="hero"><span className="eyebrow">COLLEGE RECRUITMENT, ORGANIZED</span><h1>From opportunity<br/>to <em>offer.</em></h1><p>Discover internships, manage applications and keep the complete recruitment lifecycle in one relational system.</p><div className="actions"><Link className="primary" to="/internships">Explore internships</Link><Link className="secondary" to="/login">Student login</Link></div><section className="features"><article><Briefcase/><h3>Internship discovery</h3><p>Search and filter active opportunities from verified company records.</p></article><article><Building2/><h3>Placement workflow</h3><p>Applications, interviews, offers and internship outcomes remain connected.</p></article><article><Database/><h3>Database first</h3><p>Normalized MySQL data powers every screen, metric and relationship.</p></article></section></main></Layout>}
function Internships(){const[data,setData]=useState([]),[q,setQ]=useState('');useEffect(()=>{fetch(`${API}/internships?q=${encodeURIComponent(q)}`).then(r=>r.json()).then(x=>setData(x.data||[])).catch(()=>setData([]))},[q]);return <Layout><main className="page"><h1>Internships</h1><div className="search"><Search size={18}/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search roles or skills"/></div><div className="grid">{data.map(x=><article className="job" key={x.internship_id}><small>{x.company}</small><h2>{x.title}</h2><p>{x.location} · {x.work_mode} · ₹{Number(x.stipend).toLocaleString()}</p><span>Apply by {new Date(x.application_deadline).toLocaleDateString()}</span></article>)}{!data.length&&<p className="empty">No internships loaded yet. Start the backend and seed MySQL to populate this page.</p>}</div></main></Layout>}
function Login(){const[email,setEmail]=useState(''),[password,setPassword]=useState(''),[msg,setMsg]=useState('');async function submit(e){e.preventDefault();const r=await fetch(`${API}/auth/login`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email,password})});const x=await r.json();if(r.ok){localStorage.setItem('token',x.token);setMsg(`Signed in as ${x.user.role}`)}else setMsg(x.error)}return <Layout><main className="auth"><form onSubmit={submit}><span className="eyebrow">WELCOME BACK</span><h1>Sign in</h1><label>Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><label>Password<input type="password" value={password} onChange={e=>setPassword(e.target.value)} required/></label><button>Sign in</button>{msg&&<p>{msg}</p>}</form></main></Layout>}
function App(){return <BrowserRouter><Routes><Route path="/" element={<Home/>}/><Route path="/internships" element={<Internships/>}/><Route path="/login" element={<Login/>}/></Routes></BrowserRouter>}createRoot(document.getElementById('root')).render(<App/>);
