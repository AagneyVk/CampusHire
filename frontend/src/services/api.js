export const API=import.meta.env.VITE_API_URL||'http://localhost:4000/api';export const getToken=()=>localStorage.getItem('token');export async function api(path,options={}){const response=await fetch(API+path,{...options,headers:{'Content-Type':'application/json',...(getToken()?{Authorization:`Bearer ${getToken()}`}:{}) ,...(options.headers||{})}});const data=await response.json().catch(()=>({}));if(!response.ok)throw new Error(data.error||'Request failed');return data}
